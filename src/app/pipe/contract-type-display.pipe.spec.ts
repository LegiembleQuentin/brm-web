import { ContractTypeDisplayPipe } from './contract-type-display.pipe';

describe('ContractTypeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractTypeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
